<h1>Search Results for "<%= city %>"</h1>

<% if (listings.length > 0) { %>
  <ul>
    <% listings.forEach((listing) => { %>
      <li><a href="/listings/<%= listing.id %>"><%= listing.name %></a></li>
    <% }); %>
  </ul>
<% } else { %>
  <p>No listings found for "<%= city %>".</p>
<% } %>